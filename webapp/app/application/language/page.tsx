'use client'

import { LanguageSettings } from '@/components/application/LanguageSettings'

/** Language & Region tab — see `components/application/LanguageSettings.tsx`. */
export default function LanguageTab() {
  return <LanguageSettings />
}
