'use client'

import { FeatureSettings } from '@/components/application/FeatureSettings'

/** Features tab — see `components/application/FeatureSettings.tsx`. */
export default function FeaturesTab() {
  return <FeatureSettings />
}
